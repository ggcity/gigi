[pytest]
# Only collect the V3 backend tests here. The v2 plain-script tests
# (test_chatbot.py, test_local.py, test_add_resource.py at the repo root) call
# sys.exit() and are run directly with `python`, not under pytest.
testpaths = tests
asyncio_mode = auto
