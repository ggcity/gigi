# Hugging Face Spaces installs from this file.
# Only the chatbot/runtime deps are needed (the crawler does not run on the Space).
gradio>=5
anthropic>=0.39
chromadb>=0.5
sentence-transformers>=2.7.0
huggingface_hub>=0.23
torch>=2.2