# Secrets / env
.env
*.env

# Crawled data and vector store (may contain site content + PII flags)
chroma_db*/
*.sqlite3

# V3 backend SQLite store — PII (interactions.user_query) under AR 2.16; never commit
gigi.db
gigi.db-shm
gigi.db-wal

# Logs
crawler.log
*.log

# Python
__pycache__/
*.pyc
.venv/
venv/

# Model / HF cache (usually in ~/.cache, but just in case)
.cache/