# Secrets / env
.env
*.env

# Crawled data and vector store (may contain site content + PII flags)
chroma_db*/
*.sqlite3

# V3 backend SQLite store — PII (interactions.user_query) under AR 2.16; never commit
gigi.db
gigi.db-shm
gigi.db-wal

# Logs
crawler.log
*.log

# Python
__pycache__/
*.pyc
.venv/
venv/

# Model / HF cache (usually in ~/.cache, but just in case)
.cache/

# Frontend (chat-module / Lit shell + companion) — JS build/test artifacts
node_modules/
chat-module/dist/
chat-module/test-results/
chat-module/playwright-report/
chat-module/.vite/
frontend-gigi/dist/
frontend-gigi/test-results/
frontend-gigi/playwright-report/
frontend-gigi/.vite/
coverage/